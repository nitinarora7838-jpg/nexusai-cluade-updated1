import MonitoringPageClient from './MonitoringPageClient';

export const metadata = {
  title: 'Universal Vision AI Platform | Nexus AI',
  description:
    'Plug Nexus AI into your existing CCTV infrastructure — offices, hospitals, schools, warehouses, campuses — and instantly unlock real-time security analytics, compliance automation, and autonomous incident detection.',
};

export default function MonitoringPage() {
  return <MonitoringPageClient />;
}
